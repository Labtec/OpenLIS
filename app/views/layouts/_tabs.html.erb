<div id="tabs">
  <%= navigation [:patients, t('.patients')], [:accessions, t('.requisitions')], [:diagnostic_reports, t('.diagnostic_reports')] %>
</div>

<div id="flash" class="flash">
  <%= render 'layouts/flash' %>
</div>

<%= yield(:nav) %>
