<%= form_for [:admin, @unit] do |f| %>
  <%= render 'shared/error_messages', object: @unit %>

  <div class="name">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="buttonbar">
    <%= f.submit 'Submit' %>
  </div>
<% end %>
