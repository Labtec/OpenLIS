<%= form_for [:admin, @department] do |f| %>
  <%= render 'shared/error_messages', object: @department %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p><%= f.submit 'Submit' %></p>
<% end %>
