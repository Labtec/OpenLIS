<%= turbo_stream.remove @accession %>

<%= turbo_stream.update 'quotes' do %>
  <%= render partial: "quotes/quote", collection: @patient.quotes.recent.active if @patient %>
<% end %>

<%= render_turbo_stream_flash_messages %>
